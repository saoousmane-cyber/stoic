# Vérifier Node.js (version 18.17.0 ou supérieure)
node --version

# Vérifier npm
npm --version

# Si besoin, installer Node.js depuis https://nodejs.org/
# Créer le dossier du projet
mkdir aura-and-logos
cd aura-and-logos

# Initialiser le projet Next.js avec TypeScript et Tailwind
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir
# Next.js et React (déjà inclus)
npm install next@14.1.0 react@18.2.0 react-dom@18.2.0

# Authentification Google OAuth
npm install next-auth@4.24.5 @auth/supabase-adapter

# Base de données Supabase
npm install @supabase/supabase-js@2.39.0

# Cache Redis (Upstash)
npm install @upstash/redis@1.28.0 @upstash/ratelimit@1.0.0

# Paiements Stripe
npm install stripe@14.0.0 @stripe/stripe-js@3.0.0

# UI Components
npm install @radix-ui/react-accordion@1.1.2
npm install @radix-ui/react-alert-dialog@1.0.5
npm install @radix-ui/react-avatar@1.0.4
npm install @radix-ui/react-checkbox@1.0.4
npm install @radix-ui/react-dialog@1.0.5
npm install @radix-ui/react-dropdown-menu@2.0.6
npm install @radix-ui/react-label@2.0.2
npm install @radix-ui/react-popover@1.0.7
npm install @radix-ui/react-select@2.0.0
npm install @radix-ui/react-separator@1.0.3

npm install @radix-ui/react-slot@1.0.2
npm install @radix-ui/react-switch@1.0.3
npm install @radix-ui/react-tabs@1.0.4
npm install @radix-ui/react-toast@1.1.5
npm install @radix-ui/react-tooltip@1.0.7

# Animations et styles
npm install framer-motion@11.0.0
npm install lucide-react@0.309.0
npm install tailwindcss-animate@1.0.7
npm install class-variance-authority@0.7.0
npm install clsx@2.1.0
npm install tailwind-merge@2.2.0

# Formulaires et validation
npm install react-hook-form@7.49.0
npm install zod@3.22.4

# Utilitaires
npm install bcryptjs@2.4.3
npm install sonner@1.4.0
npm install next-themes@0.2.1
npm install react-intersection-observer@9.5.0


npm install -D @types/bcryptjs@2.4.6
npm install -D @types/node@20.11.0
npm install -D @types/react@18.2.48
npm install -D @types/react-dom@18.2.18
npm install -D @typescript-eslint/eslint-plugin@6.19.0
npm install -D @typescript-eslint/parser@6.19.0
npm install -D eslint@8.56.0
npm install -D eslint-config-next@14.1.0
npm install -D prisma@5.8.0
npm install -D typescript@5.3.3
npm install -D autoprefixer@10.4.17
npm install -D postcss@8.4.33
npm install -D tailwindcss@3.4.1

# Vercel CLI pour le déploiement
npm install -g vercel

# FFmpeg pour l'audio (nécessaire pour le ducking et mixage)
# Télécharger depuis https://ffmpeg.org/download.html
# OU via winget (Windows Package Manager)
winget install FFmpeg

# Créer le fichier .env.local
copy .env.example .env.local   à voir &&&&&&&&&&&&&&&&&&&&

# Éditer le fichier avec vos clés API
notepad .env.local

-- Exécuter dans l'éditeur SQL de Supabase Dashboard

-- Table users
CREATE TABLE IF NOT EXISTS users (
  id TEXT PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  name TEXT,
  image TEXT,
  plan TEXT DEFAULT 'free',
  quota_used INTEGER DEFAULT 0,
  quota_limit INTEGER DEFAULT 5,
  quota_reset_date TIMESTAMP WITH TIME ZONE,
  stripe_customer_id TEXT,
  stripe_subscription_id TEXT,
  trial_id UUID,
  trial_expires_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Table trials
CREATE TABLE IF NOT EXISTS trials (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  user_email TEXT NOT NULL,
  status TEXT NOT NULL CHECK (status IN ('active', 'expired', 'converted', 'cancelled')),
  started_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
  converted_at TIMESTAMP WITH TIME ZONE,
  quota_used INTEGER DEFAULT 0,
  quota_limit INTEGER DEFAULT 120,
  stripe_checkout_session_id TEXT,
  stripe_customer_id TEXT,
  stripe_subscription_id TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Table usage_logs
CREATE TABLE IF NOT EXISTS usage_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  minutes_used INTEGER NOT NULL,
  plan TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Table payment_failures
CREATE TABLE IF NOT EXISTS payment_failures (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  invoice_id TEXT NOT NULL,
  amount INTEGER NOT NULL,
  currency TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Indexes
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_trials_user_id ON trials(user_id);
CREATE INDEX idx_trials_status ON trials(status);
CREATE INDEX idx_usage_logs_user_id ON usage_logs(user_id);

# Démarrer le serveur de développement
npm run dev

# Ou avec PowerShell
npm run dev

# Accéder à http://localhost:3000

# Build de production
npm run build

# Lancer en production
npm start

# Vérifier les types TypeScript
npm run type-check

# Linter
npm run lint

# Nettoyer les dépendances
npm run clean


10. Problèmes connus sur Windows
Problème	Solution
node-gyp error	npm install --global windows-build-tools
ffmpeg not found	Ajouter C:\ffmpeg\bin au PATH système
next build lente	Désactiver Windows Defender pour le dossier
Port 3000 déjà utilisé	netstat -ano | findstr :3000 puis taskkill /PID <PID> /F
Permission errors	Lancer PowerShell en Administrateur

# install.ps1 - Script d'installation complet

Write-Host "🚀 Installation de AURA & LOGOS..." -ForegroundColor Cyan

# Création du projet
mkdir aura-and-logos
cd aura-and-logos

# Initialisation Next.js
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir

# Installation des dépendances
npm install next-auth@4.24.5 @auth/supabase-adapter
npm install @supabase/supabase-js@2.39.0
npm install @upstash/redis@1.28.0 @upstash/ratelimit@1.0.0
npm install stripe@14.0.0 @stripe/stripe-js@3.0.0
npm install @radix-ui/react-accordion @radix-ui/react-alert-dialog @radix-ui/react-avatar @radix-ui/react-checkbox @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-label @radix-ui/react-popover @radix-ui/react-select @radix-ui/react-separator @radix-ui/react-slot @radix-ui/react-switch @radix-ui/react-tabs @radix-ui/react-toast @radix-ui/react-tooltip
npm install framer-motion lucide-react tailwindcss-animate class-variance-authority clsx tailwind-merge
npm install react-hook-form zod bcryptjs sonner next-themes react-intersection-observer

# Dépendances de dev
npm install -D @types/bcryptjs @types/node @types/react @types/react-dom @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint eslint-config-next prisma typescript

Write-Host "✅ Installation terminée !" -ForegroundColor Green
Write-Host "📝 N'oubliez pas de configurer .env.local" -ForegroundColor Yellow
Write-Host "🚀 Lancez avec: npm run dev" -ForegroundColor Cyan

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=votre-secret-32-caracteres-minimum

# Google OAuth
GOOGLE_CLIENT_ID=votre-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=votre-client-secret

# Supabase
SUPABASE_URL=https://votre-projet.supabase.co
SUPABASE_ANON_KEY=votre-anon-key
SUPABASE_SERVICE_ROLE_KEY=votre-service-role-key

# OpenRouter (pour Claude/GPT)
OPENROUTER_API_KEY=sk-or-votre-cles

# OpenAI (pour TTS)
OPENAI_API_KEY=sk-proj-votre-cles

# Stripe (optionnel pour Phase 1)
STRIPE_SECRET_KEY=sk_test_votre-cles
STRIPE_WEBHOOK_SECRET=whsec_votre-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_votre-cles


Résumé des commandes à exécuter dans l'ordre :

powershell
# 1. Créer le projet
mkdir aura-and-logos && cd aura-and-logos
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir

# 2. Installer les dépendances (copier-coller la section 3 + 4)
npm install next-auth@4.24.5 @auth/supabase-adapter @supabase/supabase-js@2.39.0 @upstash/redis@1.28.0 @upstash/ratelimit@1.0.0 stripe@14.0.0 @stripe/stripe-js@3.0.0 @radix-ui/react-accordion@1.1.2 @radix-ui/react-alert-dialog@1.0.5 @radix-ui/react-avatar@1.0.4 @radix-ui/react-checkbox@1.0.4 @radix-ui/react-dialog@1.0.5 @radix-ui/react-dropdown-menu@2.0.6 @radix-ui/react-label@2.0.2 @radix-ui/react-popover@1.0.7 @radix-ui/react-select@2.0.0 @radix-ui/react-separator@1.0.3 @radix-ui/react-slot@1.0.2 @radix-ui/react-switch@1.0.3 @radix-ui/react-tabs@1.0.4 @radix-ui/react-toast@1.1.5 @radix-ui/react-tooltip@1.0.7 framer-motion@11.0.0 lucide-react@0.309.0 tailwindcss-animate@1.0.7 class-variance-authority@0.7.0 clsx@2.1.0 tailwind-merge@2.2.0 react-hook-form@7.49.0 zod@3.22.4 bcryptjs@2.4.3 sonner@1.4.0 next-themes@0.2.1 react-intersection-observer@9.5.0

# 3. Installer les dev dependencies
npm install -D @types/bcryptjs@2.4.6 @types/node@20.11.0 @types/react@18.2.48 @types/react-dom@18.2.18 @typescript-eslint/eslint-plugin@6.19.0 @typescript-eslint/parser@6.19.0 eslint@8.56.0 eslint-config-next@14.1.0 prisma@5.8.0 typescript@5.3.3 autoprefixer@10.4.17 postcss@8.4.33 tailwindcss@3.4.1

# 4. Lancer le projet
npm run dev


 génère aussi le fichier install.ps1 complet pour une installation automatisée sur Windows

 # Sauvegarder la base distante
supabase db dump --local -f backup.sql

# Pousser les migrations
supabase db push

# Restaurer si nécessaire
supabase db restore

# 1. Démarrer Supabase en local
supabase start

# 2. Appliquer les migrations
supabase migration up

# 3. Générer les types TypeScript
supabase gen types typescript --local > types/supabase.ts

# 4. Se connecter au projet distant
supabase link --project-ref ton-project-ref

# 5. Pousser les migrations
supabase db push